<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0,minimum-scale=1.0, maximum-scale=1.0, user-scalable=no"/>
    <meta content="yes" name="apple-mobile-web-app-capable"/>
    <meta content="black" name="apple-mobile-web-app-status-bar-style"/>
    <meta content="telephone=no" name="format-detection"/>
    <meta content="email=no" name="format-detection"/>
    <title>领导查询登陆</title>
    <link rel="stylesheet" type="text/css" href="/stylesheets/reset.css"/>
    <link rel="stylesheet" type="text/css" href="/stylesheets/common.css">
    <link rel="stylesheet" type="text/css" href="/stylesheets/loader-query-login.css">
    <script type="text/javascript" src="/javascripts/jquery-1.8.3.min.js"></script>
    <script type="text/javascript" src="/javascripts/public.js"></script>
</head>
<body>
    <div class="login-box">
        <div class="login-header">
            <p>景区数据查询</p>
        </div>
        <form action="">
            <div class="input-code">
                <input type="text" class="w100" id="code" name="qureyCode" placeholder="请输入查询码"/>
            </div>
            <div class="query-now">
                <a href="javascipt:void" class="qurey-btn">立即查询</a>
            </div>
        </form>
    </div>
    <div class="foot-box">技术支持：深大智能·智游宝</div>
<script>
    $(function () {
        $(".qurey-btn").click(function (e) {
            e.preventDefault();
            var QueryCode = $.trim($("#code").val());
            if(QueryCode=='' ||QueryCode == 'undefined' ){
                alert("请输入查询码");
                return
            }
            $.ajax({
                type:"post",
                url:"/getQuery",
                data:{
                    QueryCode:QueryCode,
                },
                dataType:"json",
                success:function (data) {
                    console.log(data)
                    if(data.isSuc){
                        window.location.href = '/show'
                    }else{
                        alert("查询码校验失败");
                    }
                },
                error:function (data) {
                    console.log(data)
                }
            })

        })
    })
</script>
</body>
</html>
